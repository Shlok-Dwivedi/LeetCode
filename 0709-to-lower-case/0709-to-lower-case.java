class Solution {
    public String toLowerCase(String s) {
        // StringBuilder str1=new StringBuilder();
        // for(int i=0;i<s.length();i++){
        //     if(s.charAt(i)>='a' && s.charAt(i)<='z'){
        //         str1.append(s.charAt)
        //     }
        // }
        return s.toLowerCase();
    }
}